"""
Промпты для генерации психологической характеристики.

Методологическая база:
- Собчик Л.Н. — «СМИЛ (MMPI). Стандартизированный метод исследования личности»
  (модифицированные названия шкал, без стигматизации)
- Березин Ф.Б., Мирошников М.П., Соколова Е.Д. — «Методика многостороннего
  исследования личности» (3-е изд. — совместная интерпретация ММИЛ+16PF)
- Стандартные правила интерпретации MMPI/СМИЛ по Демкину (onkto.ru):
  8 шагов, интерпретация по профилю, код профиля, T > 69 / 64.
"""
from __future__ import annotations
from .models import ParsedProfile


SYSTEM_PROMPT = """Ты — опытный психолог-консультант, методолог, специалист по психодиагностике.
Твоя задача — на основе извлечённых результатов психологических тестов написать связную,
профессиональную психологическую характеристику сотрудника.

═══════════════════════════════════════════
МЕТОДОЛОГИЧЕСКАЯ БАЗА
═══════════════════════════════════════════
- Для СМИЛ (ММИЛ): шкалы интерпретируются по Собчик Л.Н. в модифицированных
  (поведенческих) названиях, а не в клинических. Это значит:
    • 1 (Hs) — «невротический сверхконтроль», НЕ «ипохондрия»
    • 2 (D)  — «пессимистичность», НЕ «депрессия»
    • 3 (Hy) — «эмоциональная лабильность», НЕ «истерия»
    • 4 (Pd) — «импульсивность», НЕ «психопатия»
    • 6 (Pa) — «ригидность», НЕ «паранойя»
    • 7 (Pt) — «тревожность», НЕ «психастения»
    • 8 (Sc) — «индивидуалистичность», НЕ «шизофрения»
    • 9 (Ma) — «оптимизм и активность», НЕ «гипомания»
  (по Березину Ф.Б. с соавт. в ММИЛ названия частично совпадают)

- Интерпретация СМИЛ ВСЕГДА по целостному профилю и коду профиля,
  а не по отдельным шкалам. По отдельной шкале — только как дополнение.
- Код профиля (напр. «2-4-7») надёжен при T > 69 (СМИЛ/ММИЛ) или T > 64 (MMPI-2).
- Различие между пиковыми шкалами должно быть ≥ 5 T-баллов.
- Для 16PF Кеттелла — нормативы по стэнам (1-10, среднее 5-6, пики 1-2 или 9-10).
- Для Big Five — нейтральные диапазоны (0-100), избегай стигматизации.
- Для DISC — поведенческие стили без диагнозов.
- Для HOLLAND — проф. предпочтения по RIASEC.
- Для MBTI — тип мышления и поведения, без оценок «лучше/хуже».
- Корпоративные оценочные шкалы (ЭФКО, вроде «Джентльмен» и «Здравомыслие») — используются для HR-отбора.
  Помни: это **не стандартные** шкалы ММИЛ/СМИЛ/MMPI, а внутренние методики работодателя.
  Интерпретируй осторожно, опираясь на название и общую логику:
    • «Джентльмен» — социально-корректное поведение, следование конвенциональным нормам,
      этикет, отсутствие агрессии и нетактичности. Высокий балл = высокая степень
      социальной желательности. Интерпретировать **только в связке со шкалой L**
      (при высоком L и высоком «Джентльмене» — возможно искажение «fake good»).
    • «Здравомыслие» — реалистичность, трезвость, практичность суждений,
      отсутствие эксцентричности и вычурности. Высокий балл = трезвый, практичный,
      ориентированный на здравый смысл. Низкий — склонность к фантазированию,
      нереалистичным суждениям.
  **Обязательно** отметь, что это корпоративные/оценочные шкалы, и что
  их интерпретация — вспомогательная, а не клиническая.

═══════════════════════════════════════════
ПРАВИЛА
═══════════════════════════════════════════
1. Язык: русский, профессиональный стиль психологического заключения.
2. Без воды, без пафоса, без «данный сотрудник является уникальной личностью».
3. Опора ТОЛЬКО на представленные данные. Не выдумывать факты.
4. Где данных не хватает — писать «данные не представлены».
5. Не ставить диагнозы, не назначать лечение, не стигматизировать.
6. Не использовать слова «шизоид», «параноик», «истеричка» и т.п. — только
   поведенческие описания.
7. Учитывать контекст: должность, возраст, стаж. Избегать шаблонных фраз.
8. Если указан код профиля СМИЛ — обязательно его проинтерпретировать.
9. Если валидность СМИЛ 'questionable' или 'invalid' — отметить это явно.
10. Рекомендации руководителю — конкретные, привязанные к данным.

═══════════════════════════════════════════
СТРУКТУРА ОТВЕТА (Markdown)
═══════════════════════════════════════════

# Психологическая характеристика: Фамилия И. О.

## 1. Общие сведения
ФИО, должность, подразделение, возраст, стаж, образование (что известно).

## 2. Использованные методики
Таблица: методика / краткие показатели.

## 3. Достоверность результатов (только если есть СМИЛ)
Оценка шкал валидности L, F, K. Если F ≥ 80 — отметить, что профиль
может быть сомнительным. Если код надёжен (T > 69) — отметить явно.

## 4. Личностный профиль
Целостное описание личности. По СМИЛ: 5 сфер (если есть):
  - **Личностные черты** — устойчивые особенности характера
  - **Основные проблемные зоны** — области наибольшего напряжения
  - **Ведущие личностные потребности** — что движет поведением
  - **Когнитивные искажения** — особенности восприятия и мышления
  - **Профессиональная пригодность** — соответствие должности

По 16PF/Big Five/DISC/HOLLAND/MBTI — отдельные подразделы с интерпретацией.

## 5. Сильные стороны
3-5 пунктов с опорой на конкретные данные.

## 6. Зоны развития и риски
3-5 пунктов.

## 7. Рекомендации руководителю
- Мотивация
- Управление
- Развитие
- Зоны внимания

## 8. Резюме
2-3 предложения.

## ⚠️ Ограничения
- Программно-сгенерированный черновик на основе результатов тестов.
- Финальное заключение — за психологом с профильным образованием.
- Методики измеряют предпочтения и актуальные состояния, не «истинную» личность.
"""


def build_user_prompt(profile: ParsedProfile) -> str:
    """Собирает user-сообщение с данными профиля."""
    parts = []

    emp = profile.employee
    parts.append("## Данные о сотруднике\n")
    parts.append(f"- ФИО: {emp.full_name or 'не указано'}")
    parts.append(f"- Должность: {emp.position or 'не указана'}")
    parts.append(f"- Подразделение: {emp.department or 'не указано'}")
    parts.append(f"- Возраст: {emp.age if emp.age is not None else 'не указан'}")
    parts.append(f"- Пол: {emp.gender or 'не указан'}")
    parts.append(f"- Стаж: {emp.tenure_years if emp.tenure_years is not None else 'не указан'}")
    parts.append(f"- Образование: {emp.education or 'не указано'}")

    parts.append("\n## Результаты методик\n")
    methods = profile.methods

    # === 16PF ===
    if methods.cattell_16pf and any(getattr(methods.cattell_16pf, f) is not None for f in methods.cattell_16pf.model_fields):
        parts.append("### 16PF Кеттелла (стэны 1-10, норма 5-6)\n")
        labels = {
            "A": "A — Замкнутость ↔ Общительность",
            "B": "B — Низкий ↔ Высокий интеллект",
            "C": "C — Эмоц. неустойчивость ↔ Устойчивость",
            "E": "E — Подчинённость ↔ Доминирование",
            "F": "F — Сдержанность ↔ Экспрессивность",
            "G": "G — Низкая ↔ Высокая совестливость",
            "H": "H — Робость ↔ Смелость",
            "I": "I — Жесткость ↔ Чувствительность",
            "L": "L — Доверчивость ↔ Подозрительность",
            "M": "M — Практичность ↔ Мечтательность",
            "N": "N — Прямолинейность ↔ Дипломатичность",
            "O": "O — Уверенность ↔ Тревожность",
            "Q1": "Q1 — Консерватизм ↔ Радикализм",
            "Q2": "Q2 — Зависимость ↔ Самостоятельность",
            "Q3": "Q3 — Низкий ↔ Высокий самоконтроль",
            "Q4": "Q4 — Расслабленность ↔ Напряжённость",
        }
        for f, label in labels.items():
            v = getattr(methods.cattell_16pf, f)
            if v is not None:
                parts.append(f"- **{label}**: {v}")

    # === Big Five ===
    if methods.big_five and any(getattr(methods.big_five, f) is not None for f in ["openness","conscientiousness","extraversion","agreeableness","neuroticism"]):
        parts.append("\n### Big Five (баллы 0-100)\n")
        bf = methods.big_five
        if bf.openness is not None:         parts.append(f"- **Открытость опыту**: {bf.openness}")
        if bf.conscientiousness is not None: parts.append(f"- **Добросовестность**: {bf.conscientiousness}")
        if bf.extraversion is not None:      parts.append(f"- **Экстраверсия**: {bf.extraversion}")
        if bf.agreeableness is not None:     parts.append(f"- **Доброжелательность**: {bf.agreeableness}")
        if bf.neuroticism is not None:       parts.append(f"- **Нейротизм**: {bf.neuroticism}")

    # === MMPI / СМИЛ (с акцентом на методологию Собчик/Березина) ===
    if methods.mmpi and any(getattr(methods.mmpi, f) is not None for f in methods.mmpi.model_fields):
        parts.append("\n### СМИЛ / ММИЛ (T-баллы, норма 50±10, код профиля — по Собчик Л.Н.)\n")

        # Валидность
        if any(getattr(methods.mmpi, f) is not None for f in ["L","F","K"]):
            parts.append("**Шкалы валидности:**")
            if methods.mmpi.L is not None: parts.append(f"- L (ложь, социальная желательность): {methods.mmpi.L}")
            if methods.mmpi.F is not None: parts.append(f"- F (достоверность, аггравация): {methods.mmpi.F}")
            if methods.mmpi.K is not None: parts.append(f"- K (коррекция, защитная установка): {methods.mmpi.K}")
            if methods.mmpi.validity:
                parts.append(f"- **Общая оценка валидности: {methods.mmpi.validity}**")

        # Базисные шкалы с модифицированными названиями Собчик
        mmpi_labels = {
            "Hs": "1 (Hs) — Невротический сверхконтроль",
            "D":  "2 (D)  — Пессимистичность",
            "Hy": "3 (Hy) — Эмоциональная лабильность",
            "Pd": "4 (Pd) — Импульсивность",
            "Mf": "5 (Mf) — Маскулинность-фемининность",
            "Pa": "6 (Pa) — Ригидность",
            "Pt": "7 (Pt) — Тревожность",
            "Sc": "8 (Sc) — Индивидуалистичность",
            "Ma": "9 (Ma) — Оптимизм и активность",
            "Si": "0 (Si) — Социальная интроверсия",
        }
        parts.append("\n**Базисные шкалы:**")
        for f, label in mmpi_labels.items():
            v = getattr(methods.mmpi, f)
            if v is not None:
                parts.append(f"- **{label}**: T = {v}")

        if methods.mmpi.code:
            parts.append(f"\n**Код профиля: {methods.mmpi.code}**")
        if methods.mmpi.profile_type:
            parts.append(f"**Тип профиля: {methods.mmpi.profile_type}**")

        # Корпоративные оценочные шкалы (ЭФКО и др.)
        if methods.mmpi.gentleman is not None or methods.mmpi.sanity is not None:
            parts.append("\n**Корпоративные оценочные шкалы (HR-отбор):**")
            if methods.mmpi.gentleman is not None:
                parts.append(f"- **«Джентльмен»** (социально-корректное поведение, этикет, следование конвенциональным нормам): T = {methods.mmpi.gentleman}")
            if methods.mmpi.sanity is not None:
                parts.append(f"- **«Здравомыслие»** (реалистичность, трезвость, практичность): T = {methods.mmpi.sanity}")

    # === DISC ===
    if methods.disc and any(getattr(methods.disc, f) is not None for f in ["D","I","S","C"]):
        parts.append("\n### DISC (баллы 0-100)\n")
        d = methods.disc
        if d.D is not None: parts.append(f"- **D — Доминирование**: {d.D}")
        if d.I is not None: parts.append(f"- **I — Влияние**: {d.I}")
        if d.S is not None: parts.append(f"- **S — Стабильность**: {d.S}")
        if d.C is not None: parts.append(f"- **C — Соответствие**: {d.C}")

    # === HOLLAND ===
    if methods.holland and (any(getattr(methods.holland, f) is not None for f in ["R","I","A","S","E","C"]) or methods.holland.code):
        parts.append("\n### HOLLAND RIASEC\n")
        h = methods.holland
        if h.R is not None: parts.append(f"- R (Реалистичный): {h.R}")
        if h.I is not None: parts.append(f"- I (Исследовательский): {h.I}")
        if h.A is not None: parts.append(f"- A (Артистический): {h.A}")
        if h.S is not None: parts.append(f"- S (Социальный): {h.S}")
        if h.E is not None: parts.append(f"- E (Предприимчивый): {h.E}")
        if h.C is not None: parts.append(f"- C (Конвенциональный): {h.C}")
        if h.code: parts.append(f"- **Код Холланда**: {h.code}")

    # === MBTI ===
    if methods.mbti and methods.mbti.type:
        parts.append(f"\n### MBTI: {methods.mbti.type}")

    # === Амтхауэр ===
    if methods.amthauer and (methods.amthauer.iq or methods.amthauer.subscales):
        parts.append("\n### Амтхауэр\n")
        if methods.amthauer.iq:
            parts.append(f"- **IQ**: {methods.amthauer.iq}")

    if profile.notes:
        parts.append("\n## Заметки парсера\n")
        for n in profile.notes:
            parts.append(f"- {n}")

    # === Сравнение с нормой ===
    try:
        from .norms import compute_deviations, format_deviations_md
        dev = compute_deviations(profile, emp)
        if dev and any(k for k in dev if not k.startswith("_")):
            parts.append("\n" + format_deviations_md(dev))
    except Exception as e:
        # Если по какой-то причине не получилось — не ломаем генерацию
        pass

    parts.append(
        "\n\nНапиши психологическую характеристику строго по указанной структуре. "
        "Опирайся только на представленные данные. Если какой-то раздел не подкреплён данными — "
        "напиши «данные не представлены» или опусти раздел. "
        "Используй модифицированные названия шкал СМИЛ (Собчик) — поведенческие, не клинические. "
        "Не стигматизируй. Интерпретация СМИЛ — по целостному профилю, не по отдельным шкалам. "
        "В разделе «Личностный профиль» обязательно учитывай блок «Сравнение с общепопуляционной нормой»: "
        "выделяй шкалы, отклоняющиеся от нормы (T < 45 или T > 55), и комментируй их значение в связке с должностью. "
        "В разделе «Зоны развития и риски» отмечай, на какие отклонения следует обратить внимание руководителю."
    )

    return "\n".join(parts)
